{
  "name": "ratchadj-integration-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">= 12.14.0",
    "npm": ">= 7.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ratchadj/integration-tests.git"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint services --max-warnings=0",
    "bootstrap": "lerna bootstrap --no-ci",
    "setup-hooks": "brew install pre-commit && pre-commit install -t commit-msg -t pre-commit -t pre-push --allow-missing-config"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "7.32.0",
    "lerna": "4.0.0"
  },
  "dependencies": {
    "jest": "28.1.3",
    "jest-cucumber": "^3.0.1",
    "jest-junit": "13.0.0"
  }
}
